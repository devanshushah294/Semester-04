import 'package:flutter/material.dart';

class Program01 extends StatelessWidget {
  const Program01({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Program - 01"),),
    body:Text("Hello"));
  }
}
